form(method='post', action=`/drive/${currentFolder.id}/deleteFolder`, class='deleteFolder form invisible')
  div(class='formTitle')
    h1 Delete folder
  .formDescription
    p This will delete the folder and all it's contents are you sure?
  div(class='formButtons')
    button(type='submit', class='btn') Delete 
 
