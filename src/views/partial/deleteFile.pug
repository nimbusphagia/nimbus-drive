form(method='post', action=`/preview/${file.id}/deleteFile`, class='deleteFile form invisible')
  div(class='formTitle')
    h1 Delete file?
  .formDescription
    p This will delete the file forever. 
  div(class='formButtons')
    button(type='submit', class='btn') Delete 
 
