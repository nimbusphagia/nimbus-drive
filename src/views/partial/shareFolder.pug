form(method='', action=``, class='shareFolder form invisible')
  div(class='formTitle')
    h1 Share folder
  .formItem 
    label(for='url') Public url:
    input(id='url', type='url', name="url", value=currentFolder.shareUrl) 
