form(method='', action=``, class='shareFile form invisible')
  div(class='formTitle')
    h1 Share file
  .formItem 
    label(for='url') Public url:
    input(id='url', type='url', name="url", value=publicUrl) 
